import AnimationSettings from './animation-settings';

const settings = new AnimationSettings({
  lineLifeTicks: 75,
  distanceCoefficient: 4,
  lineColor: '#fff',
  backgroundColor: '#000',
});

export { AnimationSettings, settings };
